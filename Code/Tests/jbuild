(jbuild_version 1)

(executables
 ((names (ThroughputLatencyTester TestLeader GatherLeaderResults GatherNoRemResults AddNumberToBlockchain))
  (modules (ThroughputLatencyTester TestLeader GatherLeaderResults GatherNoRemResults AddNumberToBlockchain))
  (modes (native))
  (libraries (lwt.unix ezirmin tc lwt ezjsonm core_extended ptime logstringcoder))))
