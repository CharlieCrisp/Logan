(jbuild_version 1)

(executable
 ((name test_single_machine_speed)
  (modules (test_single_machine_speed))
  (modes (native))
  (libraries (lwt.unix ezirmin tc lwt ezjsonm core_extended logstringcoder))))
