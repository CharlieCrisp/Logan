data = importdata("~/Documents/PartIILogs/Throughput Throughput Logs/appliedvsobserved", ' ', 1);
data = data.data;
applied = data(:,1);
observed = data(:,2);
scatter(applied,observed);
xlabel("Applied Throughput");
ylabel("Observed Throughput");
hold on
ref = 1:1:30;
plot(ref,ref);

%plot best fit
applied = [1; 2; 3; 4; 5; 6; applied];
observed = [1; 2; 3; 4; 5; 6; observed];
P = polyfit(applied, observed, 3);
yfit = P(1) * (applied .^ 3)+ P(2) * (applied .^ 2) + P(3);
plot(applied, yfit);

