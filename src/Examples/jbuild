(jbuild_version 1)

(executables
 ((names (LeaderDemo ParticipantDemo ManualParticipantDemo))
  (modules (LeaderDemo ParticipantDemo ManualParticipantDemo))
  (modes (native))
  (libraries (blockchain logstringcoder lwt))))

