(jbuild_version 1)

(executable
 ((name LogCreator)
  (modules (LogCreator LogTester))
  (modes (native))
  (libraries (lwt.unix ezirmin tc lwt ezjsonm core_extended logstringcoder))))

(executable
 ((name ParticipantRunner)
  (modules (ParticipantRunner))
  (modes (native))
  (libraries (lwt.unix ezirmin tc lwt ezjsonm core_extended logstringcoder))))
